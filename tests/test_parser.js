const assert = require('chai').assert;
const Parser = require('../src/parser');

describe('The Parser should', function () {
  var parser;
  beforeEach(function () {
    parser = new Parser();
  })

  it("be able to parse out through a docs file", function () {
    
  })

  it("be able to parse out through a remote url", function () {

  })
})