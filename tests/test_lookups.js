const assert = require('chai').assert;

describe('The Lookup should', function () {

  it("should respond with correct let/const docs", function () {

  })

  it("should respond with correct arrow function docs", function () {

  })
})